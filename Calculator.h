#pragma once

#include "my_buffer.h"
#include "EasyDebug.h"

int GetG (Buffer* buf);
int GetE (Buffer* buf);
int GetT (Buffer* buf);
int GetP (Buffer* buf);
int GetN (Buffer* buf);

int GetPower (Buffer* buf);