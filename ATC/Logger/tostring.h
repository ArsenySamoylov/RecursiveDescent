#pragma once

char* tostring(short);
char* tostring(unsigned);
char* tostring(int);
char* tostring(long);
char* tostring(long long);

char* tostring(float);
char* tostring(double);

char* tostring(char);
char* tostring(const char*);